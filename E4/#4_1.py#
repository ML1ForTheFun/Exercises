import numpy as np

w = np.random.randn(2);
x = np.array((-1, 0.3, 2));
t = np.array((-0.1, 0.5, 0.5));

def grad_desc(x, t w):
    return w;
    
def line_search(x, t, w):
   g =
   return w;

def conj_grad(x, t, w):

    return w;


#Visualization

